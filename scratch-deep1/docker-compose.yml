version: '3.8'
services:
  nielit:
    build: .
    ports:
      - "80:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - JWT_SECRET=make-this-very-long-random-string-here
    restart: unless-stopped
